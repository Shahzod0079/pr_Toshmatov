<Page x:Class="pr_26_Toshmatov.Pages.Login"
          xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
          xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
          Title="Вход в систему" Height="334" Width="400">

    <Grid Margin="20,20,20,-2" Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Вход в систему" FontSize="20" FontWeight="Bold" 
                       HorizontalAlignment="Center" Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1" Margin="10">
            <Label Content="Логин:"/>
            <TextBox x:Name="LoginBox" Height="30" Margin="0,0,0,10"/>

            <Label Content="Пароль:"/>
            <PasswordBox x:Name="PasswordBox" Height="30" Margin="0,0,0,20"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
            <Button x:Name="BtnLogin" Click="BtnLogin_Click" Content="Войти" 
                        Width="100" Height="30" Margin="5" Background="#FF46FD00" Foreground="White"/>
            <Button x:Name="BtnRegister" Click="BtnRegister_Click" Content="Регистрация" 
                        Width="100" Height="30" Margin="5" Background="#FF46FD00" Foreground="White"/>
        </StackPanel>

        <TextBlock Grid.Row="3" x:Name="ErrorText" Foreground="Red" 
                       HorizontalAlignment="Center" Margin="0,25,0,-9" 
                       TextWrapping="Wrap" Width="Auto" RenderTransformOrigin="0.5,0.5">
            <TextBlock.RenderTransform>
                <TransformGroup>
                    <ScaleTransform ScaleY="-1"/>
                    <SkewTransform/>
                    <RotateTransform/>
                    <TranslateTransform/>
                </TransformGroup>
            </TextBlock.RenderTransform>
        </TextBlock>
    </Grid>
</Page>